on:
  push:
    branches:
      - main
jobs:
  web-export:
    runs-on: ubuntu-latest
    steps:
      - name: 🏗 Setup repo
        uses: actions/checkout@v2
      - name: 🏗 Setup Node
        uses: actions/setup-node@v2
        with:
          node-version: 16.x
          cache-dependency-path: voracious/yarn.lock
          cache: yarn

      - name: 📦 Install dependencies
        working-directory: ./voracious
        run: yarn install

      - name: 🧱 Build app
        working-directory: ./voracious
        run: expo export:web
